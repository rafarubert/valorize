<%#= render :partial => "/shared/content_page" %>
<% content_for :index_info do %>
<div id="index_info">
	<% @index_infos.each do |item| %>
	<div class="column">
		<h3><%= item.title %></h3>
		<p>
			  <%= raw truncate(strip_tags(item.body), :length => 220, :omission => link_to( '...', blog_post_url(item))) %>
		</p>
	</div>
	<% end %>

</div>
<% end -%>
<div id="content">
	<div class="column">
		<%= render :partial => "pages/notices" -%>
	</div>
	<div class="column center">
		<% if @random %>
		<div class="content">
			<h4>/ Seguros</h4>
			<%= raw /<img[^>]+>/.match(@random.body).to_s %>

			<h5><%= @random.title %></h5>
			<p>
			  <%= raw truncate(strip_tags(@random.body), :length => 120) %>

				<%= link_to "Leia mais...", blog_post_url(@random) %>
			</p>
		</div>
		<% end -%>
		<div class="doubt">
			<h4>Tira dúvidas</h4>	
			<p>
				<a href="/contato">
				Entre em contato conosco. 
				</a>
			<p>
		</div>
	</div>
	<div class="column">
		<%= render :partial => "pages/tips" -%>
	</div>
	<div class="clr">&nbsp;</div>